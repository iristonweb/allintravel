/** Travel Wallet — future module (UI hooks only, backend TBD) */
export type { TravelWalletBalance, NftRouteBadge, WalletFeatureFlags } from "./types";
export { DEFAULT_WALLET_FLAGS } from "./types";
