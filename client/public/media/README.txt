Place background loop files here:
- resort-loop.webm (preferred)
- resort-loop.mp4  (fallback)

The app will try to load them as a subtle overlay in PremiumBackground.
If files are missing, the static background image + gradients will still work.

